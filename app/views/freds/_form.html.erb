<div class = "formular">
<%= form_for(@fred) do |f| %>
  <%= f.hidden_field :category_id %>
  <% if @fred.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fred.errors.count, "error") %> prohibited this fred from being saved:</h2>

      <ul>
      <% @fred.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :headline %>
    <%= f.text_field :headline %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</div>
